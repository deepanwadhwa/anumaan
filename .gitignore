# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# App working data (downloaded areas + offline tiles + saved home) — regenerated by the app
data/

# Debug sensor recordings written by the iOS app / web prototype
*.jsonl

# Archived prior project (GIS world-tokenizer) and throwaway artifacts — kept locally, not tracked
archive/

# macOS
.DS_Store
